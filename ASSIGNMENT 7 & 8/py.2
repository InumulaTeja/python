class queue:
    def __init__(self):
        self.items=[]
    def is_empty(self):
        return len(self.items)==0
    def enqueue(self,item):
        self.items.append(item)
    def dequeue(self):
        if not self.is_empty():
            return self.items.pop(0)
        return "queue is empty,no elements to pop"
    def size(self):
        return len(self.items)
    def peek(self):
        if not self.is_empty():
            return self.items[0]
        else:
            return "Queue is empty"
q=queue()
q.enqueue(5)
q.enqueue(1)
q.enqueue(9)
print(f"first popped element:{q.dequeue()}")
print(f"size of the queue:{q.size()}")
print(f"is queue empty or not:{q.is_empty()}")
print(f"peek element is :{q.peek()}")
